max: &max
  spark-version: 3.4.1
  python-version: '3.11'
  java-version: 20
  os: ubuntu-latest

latest: &latest
  spark-version: latest
  python-version: '3.11'
  java-version: 20
  os: ubuntu-latest

matrix:
  small: [*max]
  full: [*max]
  nightly: [*latest]
