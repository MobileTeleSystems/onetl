# Glob { #glob }

<!-- 
    ```{eval-rst}
    .. currentmodule:: onetl.file.filter.glob
    ```

    ```{eval-rst}
    .. autoclass:: Glob
        :members: match
    ``` 
-->

::: onetl.file.filter.glob.Glob
    options:
        members:
            - match
