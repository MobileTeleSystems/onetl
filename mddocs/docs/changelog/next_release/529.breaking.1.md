Avoid using deprecated classes from `etl-entities`, in preparation for `etl-entities==3.0.0` release.

Now `with etl_entities.process.Process(...)` context managed will not have any effect on generated HWMs and temp paths.
